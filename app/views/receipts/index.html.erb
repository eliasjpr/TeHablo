
<div class="page-header">
  <h1>
    <i class="icon-file-alt"></i>
    <%= t('receipts.titles.list') %>
    <% if current_user.is_super_agent? || current_user.is_agent? %>
        <%= link_to new_receipt_path, :class => 'btn btn-success pull-right' do %> <i class="icon-refresh"></i>
            <%= t('receipts.btn.recharge_customer') %>
        <% end %>
    <% end %>
  </h1>
</div>

<%= render "receipts/table" %>